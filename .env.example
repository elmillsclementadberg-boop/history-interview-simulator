GROQ_API_KEY=gsk_xxx
PORT=8787
HOST=0.0.0.0

# 限流配置（可按预算调整）
RATE_LIMIT_PER_MINUTE=20
DAILY_CAP_PER_IP=200
DAILY_GLOBAL_CAP=5000
JSON_BODY_LIMIT_BYTES=524288

# 可选：Upstash Redis（开启后使用持久限流；不填则回退内存限流）
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
